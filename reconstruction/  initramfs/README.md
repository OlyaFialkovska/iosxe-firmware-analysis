During extraction, the firmware image contained an initramfs archive.
After unpacking it, a standard minimal Linux boot filesystem was found, including directories such as bin, etc, lib, usr, and the init file.

These extracted contents are not stored in this repository because the full extracted tree is too large.

This layer was used for structural analysis of the early boot environment.
